import { products } from "@mrmf/shared";

export default async function seedMauryRiverMushroomFarm() {
  console.log(`Seed scaffold ready for ${products.length} Maury River Mushroom Farm products.`);
  console.log(
    "Next step: map shared product records to Medusa product, variant, sales channel, and shipping profile modules."
  );
}
